.LandingNav__container
  .LandingNav
    .LandingNavMenu
      -@page.children.each do |child_page|
        =link_to child_page.title, page_path(child_page), class: 'LandingNavMenu__Item LandingNavMenu__Item-active'